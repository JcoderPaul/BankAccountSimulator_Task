package prod.oldboy.mapper.account_mappers;

import prod.oldboy.database.entity.Account;
import prod.oldboy.dto.account_dto.AccountCreateDto;
import prod.oldboy.mapper.Mapper;

public class AccountCreateMapper implements Mapper<AccountCreateDto, Account> {

    @Override
    public Account map(AccountCreateDto object) {
        return null;
    }
}
